# kagerou に手を入れるとき

## 着手するときは issue に一言書く

**実装を始める前に、その issue にコメントする。** 番号を書くだけでよい。

```
#135 やります
```

このリポジトリは複数人・複数エージェントが同時に触る。着手の宣言が無いと、
同じ issue が二重に実装される。実際に #135 は 2 つの実装が並行して進み、
片方(#141)は CI が緑になってから捨てることになった。**捨てた側が悪いのではなく、
どちらも先に書いていなかったのが原因**。

- コメントが既にあれば、その人に任せるか、そこで分担を決める
- 数日動きが無いなら「引き取ります」と書いて進めてよい
- 手が離れたら、そう書いて手放す(黙って放置すると誰も引き取れない)

## PR を出す前に

```bash
go vet ./... && go test ./... && gofmt -l .
```

CI には**実 AWS で環境を作って壊す E2E** が構成ごとに 1 本ずつある
(3tier / ssr / multi / worker)。この 4 本と `lint` / `test` / `test-aws` が
required check。

### fork からの PR について

**fork からの PR は、そのままでは緑になりません。** E2E は OIDC でこのリポジトリの
AWS ロールを assume しますが、GitHub は fork の workflow に OIDC トークンを
渡しません(渡したら、誰でも PR を投げるだけで他人の AWS を触れてしまう)。
E2E job は fork では起動しないようになっていて、required check は
「未実行」のまま残ります。

**待たないでください。** メンテナ側で変更を同一リポジトリのブランチに持ってきて
E2E を回し、そちらをマージします。**PR を閉じる必要も、投げ直す必要もありません** —
取り込まれれば閉じます。

レビューは fork の PR 上で普通に行います。`go vet` / `go test` / `lint` は
fork でも走るので、そこは緑にしてから投げてもらえると速いです。

## commit type は「利用者に何が起きるか」で選ぶ

`chore` / `ci` / `test` は**変更履歴から除外される**設定になっている
(`release-please-config.json`)。作業の種類ではなく、**利用者から見た影響**で選ぶこと。

- `go.mod` の最低バージョンを下げた → 作業は雑務でも、利用者には
  「`go install` できるようになった」という変更。`fix`
- E2E を足すついでに生成器の穴を塞いだ → 塞いだほうが利用者に効く。`fix`

**1 つの PR に複数種類が混ざるときは、影響の大きいほうに合わせる。** squash
マージのタイトルがそのまま変更履歴に出るので、`test:` を選ぶと中の `fix:` ごと
消える。実際に 2 回やっている(#157 と #174。どちらもリリース後に手で追記した)。

判断に迷ったら、**リリースノートだけを読む人がそれを知らなくて困るか**で決める。

CI の `pr-title` job がこれを見る。PR タイトルが `test` / `ci` / `chore` なのに
中に `feat` / `fix` などが混ざっていたら、**消える変更を名指しして落ちる**。
文章で 3 回言っても止まらなかったので、機械で止めている。

## Go のバージョンを上げたくなったら

`go.mod` の `go` ディレクティブは**最低要求バージョン**で、上げると
それより古い Go を使っている人は `go install` も `go build` もできなくなる。
CI の `go-min-version` job がその最低バージョンでビルド/テストを回すので、
**新しい toolchain で `go mod tidy` して黙って上がる**ことは無いはず。

上げるなら、上げる理由(依存が要求している / 言語機能を使う)を PR に書くこと。
依存が理由なら、その依存を古いままにできないかを先に見る。

## 契約を変えるとき

`docs/CONTRACT.md` に書いてあるものは**外部が依存してよい安定インターフェース**で、
v0.x の間も後方互換を守る(変更はフィールド/タグの追加のみ)。

タグ・Environment JSON・SSM のキー・hooks の環境変数を変えるなら、
先に issue で相談する。

## IAM の権限を足したくなったら

**手で `ci-policy.json` に足して終わりにしない。** `kagerou iam-policy` の生成器に
教えること。CI の `iam-policy-drift` job がその差分を検出して落ちる。

足りない権限はローカル(admin)では絶対に再現せず、CI の絞ったロールでだけ
403 として出る。繰り返し踏んだ原因が 3 つあるので、新しい型を足すときは
`internal/iampolicy` のパッケージコメントを読むこと。
